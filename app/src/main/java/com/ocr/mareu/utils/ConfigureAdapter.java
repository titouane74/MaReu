package com.ocr.mareu.utils;

import android.content.Context;
import android.view.View;
import android.widget.TextView;

import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;

import com.ocr.mareu.di.DI;
import com.ocr.mareu.ui.fragments.MeetingRecyclerViewAdapter;
//import com.ocr.mareu.ui.MeetingRecyclerViewAdapter;

//import static com.ocr.mareu.ui.MeetingListActivity.isMenuEnabled;

/**
 * Created by Florence LE BOURNOT on 07/02/2020
 */
public class ConfigureAdapter {

    /**
     * Configuration du recyclerview
     * @param pOrder : string : indicateur de trie ou de filtre
     *
     */
    public static void configureAdapter(Context pContext, String pOrder, RecyclerView pRecyclerView) {

/*
        MeetingRecyclerViewAdapter lListAdapter = new MeetingRecyclerViewAdapter(pContext, pOrder );
        pRecyclerView.setAdapter(lListAdapter);
*/

/*
        if (lListAdapter.getItemCount() != 0) {
            isMenuEnabled = true;
        } else {
            isMenuEnabled = false;
        }
*/


    }
}
